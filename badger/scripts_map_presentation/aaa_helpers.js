const logv = (message) => {
    debugLog("" + message, 0)
}

const logi = (message) => {
    debugLog("" + message, 1)
}

const logw = (message) => {
    debugLog("" + message, 2)
}

const loge = (message) => {
    debugLog("" + message, 3)
}
